name: Playwright CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  playwright-tests:
    uses: ./.github/workflows/playwright.yml
    with:
      grafana-version: 12.1.1
      with-firefox: true
